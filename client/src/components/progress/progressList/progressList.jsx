import React from 'react';
import Progress from '../../../pages/progress/progressList/progressList';

const ProgressListComponent = () => {
    return (
        <Progress pageTitle={ '진행중인 설문' } />
    )
}

export default ProgressListComponent;