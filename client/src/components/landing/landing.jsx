import React from 'react';
import { landing } from '../../constants/const';
import Landing from '../../pages/landing/landing';

const LandingComponent = () => {
    
    return (
        <Landing landing={ landing } />
    );
}

export default LandingComponent;