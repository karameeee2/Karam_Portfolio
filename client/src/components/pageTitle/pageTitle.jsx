import React from 'react';
import PageTitle from '../../pages/pageTitle/pageTitle/pageTitle';

const PageTitleComponent = () => {

    return (
        <PageTitle pageTitle={ pageTitle } />
    )
}

export default PageTitleComponent;